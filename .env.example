# Copie este arquivo para .env e ajuste se necessário
# Comando: copy .env.example .env

# Postgres (banco de dados)
POSTGRES_USER=ghluser
POSTGRES_PASSWORD=ghlpass
POSTGRES_DB=ghldb
DATABASE_URL=postgresql://ghluser:ghlpass@localhost:5432/ghldb

# Redis (fila de tarefas)
REDIS_URL=redis://localhost:6379/0

# JWT (autenticação) - em produção use uma string longa e aleatória
JWT_SECRET=altere-isso-em-producao-use-uma-string-longa-e-aleatoria

# Produção (Render): ENV=production. CORS: deixe vazio para permitir qualquer origem,
# ou liste as URLs do frontend separadas por vírgula (ex: https://seu-app.vercel.app)
# ENV=production
# CORS_ORIGINS=
